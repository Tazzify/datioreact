function CartSum(props) {
        return (
                <div className="sum">
                        <h2 className="cart_sum">Totalt: {props.total} kr</h2>
                </div>
        );
}

export default CartSum;


